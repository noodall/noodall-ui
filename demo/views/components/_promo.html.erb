<div id="<%= slot_code %>" class="component promo <%= 'double' if expand %> <%= additional_classes %>">
  <%= link_to(image_tag(component.asset.url(promo_size(component, slot_code, expand), :jpg)), component.url)  unless component.asset.nil? %>
</div>